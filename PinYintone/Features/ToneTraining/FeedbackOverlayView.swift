import SwiftUI

/// 评分结果覆盖层：DTW 分数 + 等级 + 多语种反馈文本
struct FeedbackOverlayView: View {
    let result: FeedbackResult
    let onDismiss: () -> Void

    var body: some View {
        fatalError("TODO: 实现")
    }
}
