import SwiftUI

/// 关卡 2：声调训练（A/B 实验作用域，20 词语料库）
struct ToneTrainingView: View {
    @StateObject private var vm = ToneTrainingViewModel()

    var body: some View {
        fatalError("TODO: 实现 — 根据 GroupAssignment 路由到 ModeA / ModeB")
    }
}
