import SwiftUI

/// 模式 A（staticColor）：用色块表示声调，无实时 F0 波形
struct ModeA_StaticView: View {
    let lexeme: Lexeme
    let studentF0: [Float]
    let referenceF0: [Float]

    var body: some View {
        fatalError("TODO: 实现")
    }
}
