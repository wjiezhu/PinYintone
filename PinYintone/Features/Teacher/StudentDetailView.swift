import Charts
import SwiftUI

/// 学生详情下钻：DTW 时序折线图 + 偏误词列表
struct StudentDetailView: View {
    let student: StudentRowData

    var body: some View {
        fatalError("TODO: 实现 — LineMark DTW 时序 + 偏误词 List")
    }
}
