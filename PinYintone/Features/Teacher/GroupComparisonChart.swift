import Charts
import SwiftUI

/// A/B 两组 DTW 对比柱状图（Swift Charts，iOS 16+）
struct GroupComparisonChart: View {
    let data: GroupComparisonData

    var body: some View {
        fatalError("TODO: 实现 — BarMark，x: group, y: avgDTW")
    }
}
