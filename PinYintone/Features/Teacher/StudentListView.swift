import SwiftUI

/// 学生列表：昵称 + 练习数 + 通关率 + 最后活跃
struct StudentListView: View {
    let students: [StudentRowData]
    let onSelect: (StudentRowData) -> Void

    var body: some View {
        fatalError("TODO: 实现")
    }
}
