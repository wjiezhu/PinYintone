import SwiftUI

/// 分词编辑页：FlowLayout 展示词条芯片，支持删除/新增
struct TokenEditorView: View {
    @Binding var tokens: [String]
    let originalText: String

    var body: some View {
        fatalError("TODO: 实现 — FlowLayout + TokenChipView + 自定义词输入")
    }
}
