# Pinyintone — 不可变更的核心参数约束

## 音频采样参数
| 参数 | 值 | 原因 |
|------|----|------|
| 采样率 | **16 000 Hz** | DTW 距离矩阵和 YIN 阈值均校准于 16 kHz，改动会破坏所有评分 |
| 帧长 | **512 samples**（≈32 ms） | YIN 算法最小可检频率 = 16000/512 = 31.25 Hz，小于 75 Hz 检测下限即可 |
| 帧移 | **128 samples**（≈8 ms，75% 重叠） | 保证 F0 时间分辨率足以区分连读变调 |
| 窗函数 | **Hann window** | |

## F0 提取（YIN 算法）
| 参数 | 值 |
|------|----|
| 检测下限 | **75 Hz** |
| 检测上限 | **500 Hz**（覆盖普通话四声完整音域） |
| YIN 阈值 | **0.15**（低于此值判断为有声段） |
| 无声帧填充 | **0.0**（不做插值；DTW 对齐时忽略 0 帧） |

## DTW 评分
| 参数 | 值 |
|------|----|
| 通关线（归一化 DTW） | **≤ 0.5**（> 0.5 = 不通关） |
| 距离度量 | **欧氏距离（L2）**，作用于归一化 F0 序列 |
| 归一化方式 | **z-score per utterance** |

## 送气检测
| 参数 | 值 |
|------|----|
| 判定阈值 | **底噪 + 15 dB**（动态基线：启动后前 500 ms 采集底噪） |
| 触发率通关线 | **≥ 0.6**（600 ms 窗口内触发帧占比） |

## A/B 实验
- 作用域：**仅关卡 2（声调训练）**
- 分组：`A = staticColor`，`B = dynamicF0`
- 分组依据：**学生注册时由后端均衡随机分配**（`/student/register` 把人分到当前人数较少的组，等量时随机），保证两组人数均衡；幂等（同一 deviceID 重注册不变组）
- 离线兜底：联系不到后端时客户端本地 50/50 随机（`GroupAssignment.randomGroup()`）
- 分组存于 `profile.experimentGroup` 与后端 `users.experiment_group`，并随每条训练记录上报
- 学生注册**只需填名字**，不再使用班级码/测试码
- 关卡 3（自由文本）**不参与** A/B 分流，统一用实时 F0 可视化

## 班级码
- 格式：**6 位纯数字**
- 生成方：**服务器端**（全局唯一，禁止全同位如 000000/111111）
- **班级码一旦生成不可重置**（避免学生数据归属混乱）

## 数据上报
- 同步时机：训练结束后立即异步上报；失败标记 `synced = false`，下次启动重试
- 游客数据：上报至 `classCode = nil` 的"未绑定班级"池，**不得丢弃**（论文辅助数据）

---

## 禁止事项（任何 PR 不得违反）
1. **禁止将采样率改为 44100/48000**——破坏 YIN 和 DTW 校准
2. **禁止在关卡 3 引入 A/B 分组逻辑**——论文实验边界
3. **禁止客户端生成班级码**——必须由服务器生成以保证唯一性
4. **禁止删除游客数据上报逻辑**——论文需要"未绑定班级"数据
5. **禁止引入第三方音频/图表/NLP 库**——零第三方依赖（Accelerate / Charts / NaturalLanguage 均为系统框架）
