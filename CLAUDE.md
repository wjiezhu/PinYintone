# Pinyintone — 不可变更的核心参数约束

## 音频采样参数
| 参数 | 值 | 原因 |
|------|----|------|
| 采样率 | **16 000 Hz** | DTW 距离矩阵和 YIN 阈值均校准于 16 kHz，改动会破坏所有评分 |
| 帧长 | **512 samples**（≈32 ms） | YIN 算法最小可检频率 = 16000/512 = 31.25 Hz，小于 75 Hz 检测下限即可 |
| 帧移 | **128 samples**（≈8 ms，75% 重叠） | 保证 F0 时间分辨率足以区分连读变调 |
| 窗函数 | **Hann window** | |

## F0 提取（YIN 算法）
| 参数 | 值 |
|------|----|
| 检测下限 | **75 Hz** |
| 检测上限 | **500 Hz**（覆盖普通话四声完整音域） |
| YIN 阈值 | **0.15**（低于此值判断为有声段） |
| 无声帧填充 | **0.0**（不做插值；DTW 对齐时忽略 0 帧） |

## DTW 评分
| 参数 | 值 |
|------|----|
| 通关线（归一化 DTW） | **≤ 0.5**（> 0.5 = 不通关） |
| 距离度量 | **欧氏距离（L2）**，作用于归一化 F0 序列 |
| 归一化方式 | **z-score per utterance** |

## 送气检测
| 参数 | 值 |
|------|----|
| 判定阈值 | **底噪 + 15 dB**（动态基线：启动后前 500 ms 采集底噪） |
| 触发率通关线 | **≥ 0.6**（600 ms 窗口内触发帧占比） |

## 研究阶段（pretest / training / posttest）
- 三阶段共用**同一套算法与阈值**，差别只在词集与反馈呈现
- `pretest` / `posttest`：固定测试词集，录完**不显示分数、等级、颜色或曲线**，
  自动进入下一题；后台照常算分入库
- `training`：显示动态 F0 曲线 + 行动提示
- `phase` 随**每条记录**上报（声调 / 送气 / 自由文本三张表都有）
  - 命名以技术要求档案为准：`phase` = 研究阶段；`stage1/2/3` 在本工程里指**关卡**，两者不得混用
- 代码支持三阶段 **≠** 已完成前测/训练/后测实验；当前只有真实应用日志

## 反馈呈现（两种模式，学习者自选；**不是** A/B 分组）
- 关卡 2 的 `training` 阶段提供两种显示模式，学习者在设置里随时切换：
  - `staticColor`：颜色块 + 方向箭头 + 调号（认知负荷低）
  - `dynamicF0`：母语者目标轨迹 + 学习者轨迹（默认）
- 两种模式**通关标准完全一致**（DTW ≤ 0.5），切换不影响评分
- 这是**显示偏好**，不是实验条件：无随机分配、无反平衡、不绑定词集也不绑定人
- **不得**引入任何按人或按词集的条件分流；不得复活 `ConditionSchedule` /
  反平衡格子那套逻辑
- 词集：训练池（原 `set1` + `set2` 合并遍历）与固定测试词集 `assessment`
  **不重叠**，前后测共用测试词集
  - `set1` / `set2` 标签仅保留在语料与历史记录里，不影响出题顺序或反馈
- `assessmentSetVersion` 随测试词集记录上报；**改测试词集内容必须递增版本号**
- 记录字段现状（`schemaVersion` ≥ 3）：
  - `feedbackMode` = 该条记录当时用的显示模式（裸测为 `nil`）。
    **因为是自选的，禁止拿它做组间比较**——存在严重自选择偏差
  - `presentationOrder` **不再写入**，恒为 `nil`
  - `groupAssignment` 是历史必填列，恒写 `"n/a"`
  - `schemaVersion` < 3 的记录里 `feedbackMode` 是**随机分配**的实验条件，
    与 v3 的自选值语义不同，**不可混在一起分析**
- 历史列 `users.experiment_group` / `users.counterbalance_index` 已停止写入，
  仅保留以便旧记录可读
- 学生注册使用 **Sign in with Apple**（名字可选），不使用班级码/测试码
- 关卡 3（自由文本）不提供模式切换，统一用实时 F0 可视化

## 结果解释边界
- DTW 分数、等级、通关状态是**形成性反馈与应用日志指标**
- **禁止**把它们标记为语言能力等级，或直接当作声调习得效果的证据

## 班级码
- 作用域：**仅教师**。教师注册时服务端生成，学生端不再绑定/输入班级码
- 格式：**6 位纯数字**
- 生成方：**服务器端**（全局唯一，禁止全同位如 000000/111111）
- **班级码一旦生成不可重置**（避免班级数据归属混乱）

## 记录版本与结果状态
- `schemaVersion` / `appVersion` 随**每条记录**上报；字段增删或语义变更必须递增 `schemaVersion`
- `resultStatus`：`valid_result` / `technical_retry` / `quality_flagged`
  - 技术失败**不得**伪装成 `fail`；只提示重录、不生成发音等级
  - `failureReason` 只记匿名原因，不含任何录音内容

## 数据上报
- 同步时机：训练结束后立即异步上报；失败标记 `synced = false`，下次启动重试
- 学生数据：`classCode = nil`（学生不绑定班级），统一进入"未绑定班级"池，**不得丢弃**（论文辅助数据）

---

## 禁止事项（任何 PR 不得违反）
1. **禁止将采样率改为 44100/48000**——破坏 YIN 和 DTW 校准
2. **禁止引入任何 A/B 分组 / 条件分流逻辑**——两种显示模式是学习者自选的偏好，不是实验条件
3. **禁止客户端生成班级码**——必须由服务器生成以保证唯一性
4. **禁止删除"未绑定班级"（`classCode = nil`）学生数据上报逻辑**——论文需要该辅助数据
5. **禁止引入第三方音频/图表/NLP 库**——零第三方依赖（Accelerate / Charts / NaturalLanguage 均为系统框架）
6. **禁止在 `pretest` / `posttest` 呈现任何评价性反馈**（分数、等级、颜色、曲线）——裸测是前后测可比性的前提
7. **禁止用 `users.experiment_group` / `counterbalance_index` 决定任何呈现**——已停用的历史列
8. **禁止让训练词集与测试词集重叠**——增益比较会被"练过的词"污染
9. **禁止把技术性失败（录音质量不达标）计入发音成绩**——必须只提示重录且不生成记录
10. **禁止把自选的 `feedbackMode` 当实验条件做组间比较**——自选择偏差，不是随机分配
11. **禁止把录音启动失败静默吞掉**——`AudioEngine.start()` 抛错必须给出可执行重录提示，
    否则学习者看到的是一个按了没反应的按钮
12. **禁止向会话请求 16 kHz 采集率**（`setPreferredSampleRate`）——硬件跑原生采样率，
    由 `AVAudioConverter` 降到 16 kHz；请求 16 kHz 会造成 tap 与硬件格式不匹配（-10868）
